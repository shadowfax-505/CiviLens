# Migration-only credentials. Do not reuse the runtime database principal.
RELEASE_APP_KEY=
RELEASE_DB_HOST=managed-mysql.example.internal
RELEASE_DB_PORT=3306
RELEASE_DB_DATABASE=civiclens
RELEASE_DB_USERNAME=civiclens_migrator
RELEASE_DB_PASSWORD=
