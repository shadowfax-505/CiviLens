# Next Steps

1. Add the governed source registry and immutable acquisition ledger before any crawler runs.
2. Implement API/feed connectors, then direct-download/sitemap and static-HTML connectors with SSRF and content controls.
3. Promote approved acquisitions into the recently-indexed publisher projection without changing the Stage 2 public interface.
4. Replace unmanaged regional Earth tiles with configured licensed or self-hosted providers before production enablement.
5. Implement native-text extraction and isolated Tesseract `ben+eng` workers with one enhanced retry and abstention.
6. Implement blind review, tie-break, subject response, re-review, and versioned publication before model output is public.
7. Keep public API, semantic search, and model fine-tuning behind later ADR-backed milestones.
