# Security Rules

- Use role-based access control.
- Validate all input.
- Audit sensitive actions.
- Never log passwords, tokens, or secrets.
- Use least privilege for admin features.
- Protect private documents by default.

